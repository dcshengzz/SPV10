CREATE TABLE [dbo].[QNN_DPLY_DATASET] (
    [Id]          UNIQUEIDENTIFIER NOT NULL,
    [NumberId]    INT              IDENTITY (1, 1) NOT NULL,
    [DplyId]      UNIQUEIDENTIFIER NOT NULL,
    [SampleId]    UNIQUEIDENTIFIER NOT NULL,
    [Data]        NVARCHAR (MAX)   NOT NULL,
    [CreatedBy]   UNIQUEIDENTIFIER NULL,
    [CreatedDate] DATETIME         NULL,
    CONSTRAINT [PK_QNN_DPLY_DATASET] PRIMARY KEY CLUSTERED ([Id] ASC),
    CONSTRAINT [FK_QNN_DPLY_DATASET_DplyId] FOREIGN KEY ([DplyId]) REFERENCES [dbo].[QNN_DPLY] ([Id]) ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT [FK_QNN_DPLY_DATASET_SampleId] FOREIGN KEY ([SampleId]) REFERENCES [dbo].[QNN_SAMPLE] ([Id]) ON DELETE CASCADE ON UPDATE CASCADE
);

